mutation CreateTodo($input: CreateTodoInput!){
    createTodo(input: $input){
        id
        createdAt
        description
        complete
    }
}

input CreateTodoInput {
    description: String
}