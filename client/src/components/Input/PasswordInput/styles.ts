import styled from 'styled-components/native';

export const PasswordInputContainer = styled.View`
  padding-right: 50px;
`;

export const PasswordInputToggleButtonContainer = styled.View`
  position: absolute;
  right: 0px;
  bottom: 0px;
  border-bottom-width: 1px;
  border-bottom-color: rgba(0, 0, 0, 0.4)
  padding: 14px 12px 6px;
`;

export const PasswordInputToggleButton = styled.Image`
  opacity: 0.5;
  width: 26px;
  height: 26px;
`;
